disable_all_formatting = true
# apparently `disable_all_formatting` is going to be deprecated in the future
# when `ignore` is stabilised, but for the time being the `ignore` directive
# gives us a warning, so.
#
# uncomment at some point in the future??
#
# ignore = ["/"]
